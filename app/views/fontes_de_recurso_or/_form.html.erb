<%= form_for(@fonte_de_recurso) do |f| %>
  <% if @fonte_de_recurso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fonte_de_recurso.errors.count, "error") %> prohibited this fonte_de_recurso from being saved:</h2>

      <ul>
      <% @fonte_de_recurso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
